config:
  {% if cookiecutter.include_aws == "yes" %}aws:region: {{cookiecutter.aws_region}}
  {% endif -%}
  {{cookiecutter.project_slug}}_example:provider_param1: nuage
  {{cookiecutter.project_slug}}_example:provider_param2: some_value
